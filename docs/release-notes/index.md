# Release notes

This page is bound to the repository `CHANGELOG.md`. Stage 4 (P4) adds an
in-app "what's new" surface that reads the same source, and Stage 7 wires
the build to keep this page in sync with each release.

> Bound to `CHANGELOG.md` in **Stage 7**; in-app surface in **Stage 4 (P4)**.
