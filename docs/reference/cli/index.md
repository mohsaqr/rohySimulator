# CLI & ops reference

Generated from `bin/` + `scripts/`. `rohy-update` subcommands and exit
codes, `migrate.js`, the `seed*` scripts, `retention-sweep.js`, and the
`audit-*.sh` suite.

> Generated in **Stage 2 (R4)**. Placeholder until then.
